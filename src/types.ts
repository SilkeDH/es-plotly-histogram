export interface PlotlyOptions {
  mode: boolean;
  btseries: number;
  showLegend: boolean;
  labelAngle: number;
  margin: number;
  selecField: string;
  bgColor: string;
  YaxisLabel: string;
  XaxisLabel: string;
}
